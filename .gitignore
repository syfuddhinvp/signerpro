__pycache__/
*.py[cod]
.pytest_cache/
.venv/
node_modules/
.next/
dist/
coverage/
uploads/
backend/uploads/
frontend/.next/
frontend/node_modules/
.env
.env.*
!.env.example
!.env.prod.example
.DS_Store


# TypeScript incremental build cache — machine-local, must not be committed
*.tsbuildinfo
frontend/tsconfig.tsbuildinfo

# Package-manager sources of truth other than npm (see AUDIT_REPORT.md finding 41)
package-lock.json
yarn.lock
bun.lockb

# Production environment file (see .env.prod.example)
.env.prod

# Local-only demo integration app (a third-party consumer of the developer
# APIs, used to check those surfaces actually work against a running stack).
# Deliberately untracked: it is a test harness, not part of the product.
/demo-app/
